import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

Widget carouselWidget1(context, image) {
  return Container(
    height: 150,
    width: MediaQuery.of(context).size.width,
    decoration: BoxDecoration(
      color: Colors.black87,
      border: Border.all(color: Colors.white),
    ),
    child: Image.asset(
      image,
      fit: BoxFit.fill,
    ),
  );
}

Widget carouselWidget2(context, image) {
  return Container(
    height: 150,
    width: MediaQuery.of(context).size.width,
    decoration: BoxDecoration(
      color: Colors.black87,
      border: Border.all(color: Colors.white),
    ),
    child: Image.asset(
      image,
      fit: BoxFit.fill,
    ),
  );
}

Widget carouselWidget3(context, image) {
  return Container(
    height: 150,
    width: MediaQuery.of(context).size.width,
    decoration: BoxDecoration(
      color: Colors.black87,
      border: Border.all(color: Colors.white),
    ),
    child: Image.asset(
      image,
      fit: BoxFit.fill,
    ),
  );
}

Widget carouselWidget4(context, image) {
  return Container(
    height: 150,
    width: MediaQuery.of(context).size.width,
    decoration: BoxDecoration(
      color: Colors.black87,
      border: Border.all(color: Colors.white),
    ),
    child: Image.asset(
      image,
      fit: BoxFit.fill,
    ),
  );
}

Widget carouselWidget5(context, image) {
  return Container(
    height: 150,
    width: MediaQuery.of(context).size.width,
    decoration: BoxDecoration(
      color: Colors.black87,
      border: Border.all(color: Colors.white),
    ),
    child: Image.asset(
      image,
      fit: BoxFit.fill,
    ),
  );
}

Widget carouselWidget6(context, image) {
  return Container(
    height: 150,
    width: MediaQuery.of(context).size.width,
    decoration: BoxDecoration(
      color: Colors.black87,
      border: Border.all(color: Colors.white),
    ),
    child: Image.asset(
      image,
      fit: BoxFit.fill,
    ),
  );
}

Widget carouselWidget7(context, image) {
  return Container(
    height: 150,
    width: MediaQuery.of(context).size.width,
    decoration: BoxDecoration(
      color: Colors.black87,
      border: Border.all(color: Colors.white),
    ),
    child: Image.asset(
      image,
      fit: BoxFit.fill,
    ),
  );
}
